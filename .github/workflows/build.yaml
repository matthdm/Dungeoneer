name: Build and Upload Go Binary (Linux & Windows)

on:
  workflow_dispatch:
    inputs:
      build_name:
        description: 'Name of the build binary'
        required: true
        default: 'myapp'

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        goos: [linux, windows]
        goarch: [amd64]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Build Go binary for ${{ matrix.goos }}-${{ matrix.goarch }}
        run: |
          export GOOS=${{ matrix.goos }}
          export GOARCH=${{ matrix.goarch }}
          BINARY_NAME="${{ inputs.build_name }}-${GOOS}-${GOARCH}"
          if [ "$GOOS" = "windows" ]; then
            BINARY_NAME="${BINARY_NAME}.exe"
          fi
          go mod tidy
          go build -o $BINARY_NAME .
          echo "Built $BINARY_NAME"
        shell: bash

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.build_name }}-${{ matrix.goos }}-${{ matrix.goarch }}
          path: ${{ inputs.build_name }}-${{ matrix.goos }}-${{ matrix.goarch }}*
          retention-days: 90